import express from "express";
import service from "../Services/raffleService.js";

const route = express.Router();

route.get("/raffle", async (request, response) => {


})

route.post("/createRaffle", async (request, response) => {

    const [id_user, name, item] = request.body

    try{

        await
        
        await

        item.forEach(a => {

            await

        })


    }catch(err){

    }
})